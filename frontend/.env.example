# Copy this file to .env and adjust if needed.
# Leave VITE_API_BASE_URL unset to use the Vite dev proxy (see
# vite.config.js) which forwards /api to the C++ backend automatically.
VITE_API_BASE_URL=/api