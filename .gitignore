# Secrets — never commit real .env files, only .env.example
.env
backend/.env
frontend/.env

# Backend build output
backend/build/
backend/logs/

# Frontend
frontend/node_modules/
frontend/dist/

# vcpkg (if cloned locally instead of system-installed)
vcpkg/
vcpkg_installed/

# OS/editor cruft
.DS_Store
*.swp
.vscode/*
!.vscode/extensions.json